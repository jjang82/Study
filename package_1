
# 필요한 패키지 설치---------------------
# devtools, roxygen2
if (! ("devtools" %in% rownames(installed.packages()))) { install.packages("devtools") }
base::require("devtools")

if (! ("roxygen2" %in% rownames(installed.packages()))) { install.packages("roxygen2") }
base::require("roxygen2")

# 패키지 working directory 생성-----------------
setwd("D:/#.Secure Work Folder/DX-LSS-Project/new")

#dvtools를 활용한 Package 제작 --------------
devtools::create("test")


#ESCRIPTION 파일은 패키지에 대한 기본정보가 담겨져 있는데 위에서 봤던 R Console 내용과 같습니다. 이 파일을 수정하여 기본적인 패키지 정보를 수정할 수 있습니다.

#NAMESPACE라는 파일도 보이는데 다음과 같이 파일을 열어보면 다음과 같이 몇 글자 적혀 있습니다:
#NAMESPACE 파일은 roxygen2가 자동으로 생성하는 파일이며, 배포하고자 하는 패키지가 의존하는 라이브러리와 새로 내보내어질 함수 등을 정의합니다.

# test 디렉터리 내 생성된 test/R 디렉터리에 필요한 함수 혹은 Code 파일들을 작성합니다:
# ex) 사칙연산 추가


# 도큐먼트 생성 ----------------
devtools::document()

# man폴더 하위에 각 R 소스파일에 대한 Rd파일이 제작되어 있는 것을 확인할 수 있음.


# 로컬 패키지 설치하기 ------------------
# devtools::install()함수를 이용하여 개발한 로컬 패키지를 설치
devtools::install(pkg="R")

# 패키지 로딩 및 테스트
base::require("test")

a = 8
b = 4
test::Add(a,b)
add
help(add)
